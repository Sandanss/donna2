# Feature Backlog

> **How this works:** Nick describes features in plain English, Claude parses them into rows. David fills in the Difficulty column. Changes are tracked in git history.

| # | Feature | Description | Rationale | Priority | Urgency | Difficulty (David) | Status |
|---|---------|-------------|-----------|----------|---------|-------------------|--------|
| 1 | Admin Dashboard Revamp | Scope and build out a revamp of the admin dashboard for tracking individual user usage of Donna. Should surface per-user metrics: missed vs answered call rates, average call duration, reminder usage, topics discussed, and engagement trends. Requires a scoping discussion between Nick and David on layout and data views before any build work begins. | Once we have a few dozen users, we need user-level visibility into how each senior is engaging with Donna — not just aggregate stats. This will inform product decisions and help us identify at-risk users or underutilized features. | Medium | Low | — | Not Started |
| 2 | Dynamic Reminder Creation & Call Scheduling (Ask Donna in App) | Port and improve the existing "Ask Donna" chat feature from the website into the mobile app. The web version has known rough edges — the app version should improve on the UX, especially for bulk operations (e.g., scheduling a call 5 minutes before all 82 Dallas Mavericks games with a "tune in" reminder, in one shot). Key learnings from the app build should then be backported to the website. | Caregivers need a fast, natural way to set call times and reminders — especially in bulk. The current web "Ask Donna" is a start but isn't polished enough for the primary caregiver surface (the app). Making this seamless reduces friction and increases reminder adoption. | Medium-High | Medium | — | Not Started |
| 3 | Direct Data Feeds for Factual Queries | Investigate and implement direct data feeds (sports scores/schedules, weather, etc.) as an alternative or supplement to web search (Tavily) for questions with definitive answers. Current web search is competitive but unreliable for real-time factual queries (e.g., game times) — Alexa handles these correctly nearly every time using structured data feeds rather than web scraping. Requires research into which query categories seniors use most, available data APIs, and cost/complexity trade-offs. | To position Donna as a viable Siri/Alexa equivalent for seniors, she needs to reliably answer direct factual questions (game times, weather, stock prices, etc.). Web search works for open-ended queries but isn't accurate or fast enough for structured data. Understanding what seniors actually ask Donna will help prioritize which feeds to integrate first. | Medium | Low-Medium | — | Not Started |
| 4 | Timezone Management | Audit and fix how timezones work across the app and website. All scheduled call times should be in the senior's timezone by default. Known issues: some accounts default to Central time despite onboarding specifying Eastern or Pacific locations. Cross-timezone scenario must work correctly — if a caregiver on Eastern sets a call for 12pm for their West Coast senior, the senior should be called at 12pm Pacific (not 9am). Requires end-to-end audit of timezone handling in onboarding, scheduling, Ask Donna, and display layers. | Important standalone, but also a dependency of the Ask Donna mega-feature — dynamic scheduling via chat creates more edge cases when timezone logic is unreliable. Needs to be solid before bulk scheduling features ship. | Low-Medium | Low-Medium | — | Not Started |
